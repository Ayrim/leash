
$('#ExistingConnections').html('<%= escape_javascript(render template: "connections/show_connections") %>');
$('#PendingConnections').html('<%= escape_javascript(render template: "connections/show_pending_connections") %>');

$('#pendingConnectionsTitle').html('Pending <% if @pending_connections.count > 0 %> (<%= @pending_connections.count %>)<% end %>');