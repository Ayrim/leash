
$('#photoNoAlbumList').html('<%= escape_javascript(render template: "images/index_list_photos") %>');
if ($('#edit_picture_form').length > 0)
	$('#edit_picture_form')[0].reset();

ShowHideEditPicture(false);

$('.slider').slider();